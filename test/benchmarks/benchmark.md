# GRiD Performance Benchmarks

*This file is a committed curated snapshot. To update it, run:*

```bash
python test/benchmarks/run_benchmarks.py
```

*Results are auto-generated by `generate_report.py` from the raw JSON in `results/`.*
*After reviewing the diff, commit both `benchmark.md` and the relevant `results/*.json` snapshot.*

---

*No results recorded yet. Run the benchmark suite on a reference machine to populate this file.*
